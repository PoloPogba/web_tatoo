                       <div class="page-header" >

  <div class="row">
  <div class="col-md-6 px-0" >
    <div class="card card-fashion card-container ">
      <h4 class="card-title">The view from south Texas in one Picture</h4>
      <div class="card-body">
         
         <div class="container ">
                       
                      
        </div>
      </div>
    </div>
  </div>
  <div class="col-md-6 px-0">
    <%= image_tag "img1.jpg", :class=>"card card-fashion card-background" %>
  </div>

</div>
</div>
                       <%= form_tag guests_path(:number => 1), method: :post do %>
                       <%= submit_tag 'Un Blog', class:"btn btn-warning btn-round btn-lg" %>
                       <% end %>

                       <%= form_tag guests_path(:number => 2), method: :post do %>
                       <%= submit_tag 'Un CV', class:"btn btn-warning btn-round btn-lg" %>
                       <% end %>

                       <%= form_tag guests_path(:number => 3), method: :post do %>
                       <%= submit_tag 'Un PortFolio ', class:"btn btn-warning btn-round btn-lg" %>
                       <% end %>
                        <%= form_tag guests_path(:number => 4), method: :post do %>
                       <%= submit_tag 'Site Vitrine', class:"btn btn-warning btn-round btn-lg" %>
                       <% end %>

                       <%= form_tag guests_path(:number => 5), method: :post do %>
                       <%= submit_tag 'E-commerce ', class:"btn btn-warning btn-round btn-lg" %>
                       <% end %>

                       <%= form_tag guests_path(:number => 6), method: :post do %>
                       <%= submit_tag 'Quelque chose de créatif ', class:"btn btn-warning btn-round btn-lg" %>
                       <% end %>
                        <%= form_tag guests_path(:number => 7), method: :post do %>
                       <%= submit_tag 'Quelque chose de spécifique', class:"btn btn-warning btn-round btn-lg" %>
                       <% end %>

                       <%= form_tag guests_path(:number => 8), method: :post do %>
                       <%= submit_tag 'Quelque chose de fun ', class:"btn btn-warning btn-round btn-lg" %>
                       <% end %>

                       





